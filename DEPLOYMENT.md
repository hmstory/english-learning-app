# 🚀 GitHub Pages 배포 가이드

이 프로젝트를 GitHub Pages로 배포하는 방법을 안내합니다.

## 1단계: GitHub 저장소 생성

1. GitHub에 로그인합니다
2. 우측 상단의 `+` 버튼을 클릭하고 `New repository`를 선택합니다
3. 저장소 이름을 입력합니다 (예: `english-learning-app`)
4. `Public` 또는 `Private`을 선택합니다
5. `Add a README file`은 체크하지 않습니다 (이미 README.md가 있으므로)
6. `Create repository`를 클릭합니다

## 2단계: 로컬에서 Git 초기화 및 푸시

터미널에서 다음 명령어를 실행합니다:

```bash
# 프로젝트 디렉토리로 이동
cd english-learning-app

# Git 초기화
git init

# 모든 파일 추가
git add .

# 첫 커밋
git commit -m "Initial commit: 영어 원서 학습 앱"

# GitHub 저장소를 원격 저장소로 추가 (YOUR_USERNAME과 YOUR_REPO_NAME을 실제 값으로 변경)
git remote add origin https://github.com/YOUR_USERNAME/YOUR_REPO_NAME.git

# 메인 브랜치로 이름 변경 (필요한 경우)
git branch -M main

# GitHub에 푸시
git push -u origin main
```

## 3단계: GitHub Pages 활성화

1. GitHub 저장소 페이지로 이동합니다
2. 상단 메뉴에서 `Settings`를 클릭합니다
3. 왼쪽 사이드바에서 `Pages`를 클릭합니다
4. `Source` 섹션에서:
   - `Branch`를 `main`으로 선택
   - `/ (root)` 폴더 선택
5. `Save` 버튼을 클릭합니다

## 4단계: 배포 확인

1. 몇 분 후 (보통 1-2분), 저장소 페이지 상단에 "Your site is live at..." 메시지가 표시됩니다
2. 또는 `Settings` → `Pages`에서 배포된 URL을 확인할 수 있습니다
3. URL 형식: `https://YOUR_USERNAME.github.io/YOUR_REPO_NAME`

## 업데이트 배포

코드를 수정한 후 다음 명령어로 업데이트를 배포할 수 있습니다:

```bash
git add .
git commit -m "업데이트 내용 설명"
git push
```

GitHub Pages는 자동으로 업데이트됩니다 (몇 분 소요).

## 문제 해결

### 페이지가 표시되지 않는 경우

1. `Settings` → `Pages`에서 배포 상태를 확인합니다
2. 브라우저 캐시를 지웁니다 (Ctrl+Shift+R 또는 Cmd+Shift+R)
3. 몇 분 더 기다려봅니다 (배포에 시간이 걸릴 수 있습니다)

### 404 에러가 발생하는 경우

1. `index.html` 파일이 루트 디렉토리에 있는지 확인합니다
2. 파일명이 정확한지 확인합니다 (대소문자 구분)
3. `.nojekyll` 파일이 있는지 확인합니다

### 스타일이나 스크립트가 작동하지 않는 경우

1. 브라우저 개발자 도구(F12)에서 콘솔 에러를 확인합니다
2. 파일 경로가 상대 경로로 올바르게 설정되어 있는지 확인합니다
3. 모든 파일이 GitHub에 푸시되었는지 확인합니다

## 커스텀 도메인 사용 (선택사항)

1. `Settings` → `Pages`에서 `Custom domain` 섹션을 찾습니다
2. 도메인을 입력하고 저장합니다
3. DNS 설정을 도메인 제공업체에서 변경합니다
4. 자세한 내용은 [GitHub Pages 공식 문서](https://docs.github.com/en/pages/configuring-a-custom-domain-for-your-github-pages-site)를 참조하세요

---

배포에 문제가 있으면 GitHub Issues에 문의하거나 저장소 관리자에게 연락하세요.

